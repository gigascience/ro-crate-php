<?php

namespace ROCrate;

/**
 * Stores the structural information of a ro-crate-metadata.json as a crate object
 */
class ROCrate
{
    // check py js implementation
    // constructors
    // get set
    // perhaps context string + smaller objects of data entity class with types: root, usual, contextual
    // str in ROCrate or just a data entity object for ROCrate Metadata Descriptor
    // hold a list of sorted data enttity objects based on internal index

    // require ? probably just once due to docker image

    
}